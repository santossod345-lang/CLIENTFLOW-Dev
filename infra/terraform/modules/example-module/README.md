Example Terraform module

This module is a placeholder demonstrating how to structure reusable modules.

Inputs:
- `environment` - environment string

Replace resources with concrete implementations (VPC, RDS, IAM, etc.) as needed.
