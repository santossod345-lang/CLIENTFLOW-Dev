# ============================================================
# ClientFlow — Environment Variables
# Architecture: Backend on Railway · Frontend on Vercel
# ============================================================

# ============================================================
# RAILWAY (Backend) — set these in Railway → Variables
# ============================================================

# DATABASE (Railway PostgreSQL plugin sets this automatically)
DATABASE_URL=postgresql://user:password@host:5432/clientflow

# JWT secret — generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your_secret_key_here

# Set to production on Railway
ENVIRONMENT=production

# Your Vercel frontend URL (Railway needs this for CORS)
# All *.vercel.app subdomains are also allowed automatically (preview deployments)
ALLOWED_ORIGINS=https://clientflow-dev-one.vercel.app

# Token expiry
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# Redis (optional — Railway Redis plugin sets REDIS_URL automatically)
REDIS_URL=redis://localhost:6379/0

# AI Assistant (optional)
OPENAI_API_KEY=sk-...

# ============================================================
# VERCEL (Frontend) — set these in Vercel → Settings → Environment Variables
# ============================================================

# Your Railway backend URL (no trailing slash) — REQUIRED for the frontend to work
VITE_API_URL=https://your-app.up.railway.app

# ============================================================
# LOCAL DEVELOPMENT — create a .env file with these values
# ============================================================

# Leave DATABASE_URL and POSTGRES_HOST unset to use SQLite automatically
# SQLITE_PATH=clientflow.db   # optional, defaults to clientflow.db

# For local dev without Railway, ENVIRONMENT=development
# ENVIRONMENT=development
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
